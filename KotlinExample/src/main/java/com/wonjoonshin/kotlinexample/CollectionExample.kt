package com.wonjoonshin.kotlinexample

class CollectionExample {
    // Collection에는 세가지 종류가 있다
    // List -> 배열
    // Set -> 중복된 요소가 없다. 인덱스가 없다.
    // Map -> 키값이 중복된 요소가 없다. Key, Value 값으로 구성된 요소를 넣어준다.
}

fun main(){
    //https://brunch.co.kr/@springboot/57
}